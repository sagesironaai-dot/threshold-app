━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ DOMAIN: Integration · INT · standalone ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ GROUP: standalone SEEDS: s02 · s05 · s10 ORIGIN AFFINITY: null THRESHOLD AFFINITY: th05 · th08 PILLAR: p01 PURPOSE: Single entry point for all source material into the archive. Every document receives provenance, parsing, tagging, and deposit routing here before existing anywhere else. The provenance chain established at INT is the chain of custody for all downstream analysis and future model training. STRUCTURAL RULES: 1. Every deposit receives provenance before routing. 2. Split deposits named and handled independently — never collapsed into a single target. 3. Deferrals held open — never forced to resolution. 4. Routing derived from SOT only — never guessed, never assumed. 5. Parsing confidence reported honestly at retirement — a future model reads this to weight its own interpretation of the record. 6. MTM (07) never receives deposits — route to the appropriate Axis lens page instead. 7. No entry reaches any section without clearing INT first. CONNECTS TO: Feeds → all sections except MTM (07) Draws from → SOT section map SIGNAL PROFILE: Primary — Truth · Anchor · Consent Secondary — Sovereignty · Memory/Recall ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━