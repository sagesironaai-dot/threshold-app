{
  "version": 2,
  "last_updated": "2026-04-08",
  "status": "REBUILD PENDING — all old-build entries cleared. Map will be rebuilt during Step 4 (core files phase) when FastAPI routes, Svelte components, and backend services are written. Until then, use SCHEMA_PROTOCOL.md section 2 for manual cascade analysis.",
  "note": "Living document. Every new file gets an entry when it is created. Every new import, event connection, or shared constant gets added to affects[]. Update this map as part of the work unit that introduces the dependency — never after the fact.",
  "files": {}
}
